### Reverse a string

1. reversed = ""
2. use a loop , i = string.length - 1 , i>=0 , i--
3. reversed += string[i]
4. reversed now contains the reversed string

another method

- string -> split("") 
- now it gives an array of characters
- reverse() the array ->
- then join the array to make it as a string using join("");

---
